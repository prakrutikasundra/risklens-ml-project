services:
  - type: web
    name: loan-default-risklens
    env: python
    buildCommand: pip install -r requirements.txt && python -m src.train
    startCommand: gunicorn --workers 2 --bind 0.0.0.0:$PORT backend.server:app
    envVars:
      - key: FLASK_ENV
        value: production
